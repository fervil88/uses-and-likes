<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:appcompat="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.cordova.jokerapp.activities.MainActivity">

    <item
        android:id="@+id/action_refresh"
        android:title="@string/refresh"
        android:icon="@mipmap/ic_menu_refresh"
        android:alphabeticShortcut="r"
        android:orderInCategory="1"
        android:showAsAction="always"/>

   <group android:checkableBehavior="all">
        <item android:id="@+id/option_dirty_joke"
            android:title="@string/dirty_joke" />
    </group>

    <group android:checkableBehavior="none">
        <item
            android:id="@+id/option_feedback"
            android:title="@string/feedback"
            android:enabled="false"/>

        <item
            android:id="@+id/option_about"
            android:title="@string/about"
            android:enabled="false"/>
    </group>

   <!--  <item
        android:id="@+id/search"
        android:title="@string/search"
        appcompat:actionViewClass="android.support.v7.widget.SearchView"
        appcompat:showAsAction="always" /> -->
</menu>
